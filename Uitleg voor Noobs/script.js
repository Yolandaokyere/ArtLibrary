// volgorde van schrijven. voor beter overzicht
// 1. variables (aka bindings), boven de scopes
// variabelen boven de scopes
// vanaf de global kun je niet naar de local scope
const endpoint = '' 
let iterator = 0
// const kun je niet veranderen. 

// 2. the story (hier wordt de functie uitgevoerd je die je bij 3 hebt uitgeroepen)
something ()


// 3. functions Ihier declareedje de functies)

// voorbeeld
function something () {
   // 1. variables (aka bindings), boven de scopes
   const target = '' 
   function $ () {
    //    met dollar element zoek je iets in de dom
    ipv return documen.queryselector (Element)
    

   }

//    data ophalen uit een url
//  2. in the story
// met fetch doe dit asynchronous
fetch (endpoint)
.then(function(response){
    return response.json()
})
.then(function(data) {
    console.log(gifje.data[0].url)
    const list = $('ul')
})



// promises volgende les 
// return terugkoppeling
// want je verzendt data (.then) en om die terug te koppelen naar resultaat moet je de return functie oproepen. 
// data ophalen met console.log die laat weten wat er gebeurt. 

// van js naar html
// 
//  [] = object
//  {} = array 